node_modules/
release/
dist-web/
_site/

# Electron/Tauri build icon (regenerated at build time)
resources/icon.png

# NOTE: favicon-*.png and og-image.png ARE committed so GitHub Pages can serve
# them directly when deploying from a branch (no CI generation step required).

# Tauri build output
src-tauri/target/
src-tauri/gen/

*.log
